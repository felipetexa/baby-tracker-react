import styled from 'styled-components'

export const HeaderWrapper = styled.nav`
  display: flex;
  justify-content: space-between;
  align-items: center;

  padding-inline: 4rem;

  height: 8rem;
  box-shadow: 2px 2px 2px 1px rgba(0, 0, 0, 0.2);
`

export const Title = styled.h1`
  font-size: 3.8rem;
  font-weight: 400;
  margin-inline: auto;
  padding-left: 15rem;
`

export const GenderTheme = styled.div`
  font-size: 2rem;
`
